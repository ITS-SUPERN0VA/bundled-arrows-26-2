name: Build mod on: push: workflow_dispatch: jobs: build: runs-on: ubuntu-latest steps: - uses: actions/checkout@v4 - name: Set up JDK 25 uses: actions/setup-java@v4 with: java-version: '25' distribution: 'temurin' - name: Make gradlew executable run: chmod +x gradlew - name: Build run: ./gradlew build --stacktrace - name: Upload jar uses: actions/upload-artifact@v4 with: name: bundled-arrows-jar path: build/libs/*.jar
